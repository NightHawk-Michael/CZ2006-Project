<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.clarissapink.leafapp">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".views.MainPageView"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".views.DebtRepaymentView"
            android:label="@string/title_activity_debt__repayment"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".views.DebtRepaymentResultView"
            android:label="@string/title_activity_view__debt__repayment"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".views.ApplicableGrantResultView"
            android:label="@string/title_activity_applicable_grant_result"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".views.ApplicableGrantView"
            android:label="@string/title_activity_applicable_grant"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".views.FlatAvailableView"
            android:label="@string/title_activity_flat_available"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".views.FlatAvailableResultView"
            android:label="@string/title_activity_flat_available_result"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".views.AffordableFlatView"
            android:label="@string/title_activity_affordable_flat"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".views.AffordableFlatResultView"
            android:label="@string/title_activity_affordable_flat_result"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapDisplay"
            android:label="@string/title_activity_map_display"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>
